#include <stdio.h>
#include <string.h>

int caseno, num_cases;

int
main()
{
    scanf("%d", &num_cases);

    for (caseno = 1; caseno <= num_cases; caseno++) {

        /*
        if (caseno > 1)
            putchar('\n');
        //*/

    }

    return 0;
}
